type Query {
  info: String!
  feed: [Link!]!
#   ind_link(id: ID!): Link
}

type Mutation {
    post(url: String!, description: String!) : Link!
    update(url: String!, description: String!) : Link!
    # deleteLink(id: ID!): Link

}

type Link {
    id: ID!
    description: String!
    url: String!
}

